export default class AcademicMethod {
    constructor(id, specialization, academicDiscipline) {
        this.id = id;
        this.specialization = specialization;
        this.academicDiscipline = academicDiscipline;
    }
}